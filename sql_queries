1. write a queries to retrive the list of employee working in the same department

select distinct e.empid, empfname, e.department from employeeinfo e employeeinfo e1 where e.department=e1.department and e.empid !=e1.empid

2. write a queries to fetch detils of employee whoes emplname end with  an alphabet 'A' and contain five alphabets

select * from emaployeeonfo where emplname LIKE '____a'

oprator  -> LIKE 
wildcard --> '_'


1. Find the 2nd highest salary from an employee table

SELECT MAX(salary)
FROM employees
WHERE salary < (SELECT MAX(salary) FROM employees);


# write the sql query for thoes employee name and manager name whoes salary is greater then manager salary
Employee
name, id, salry, manager_id,
om     1  10k.     2
jhon   2  5k       3
kevin  3. 15k      null

2. Select e.name AS employee_name
          e.salary AS employee_salary
          m.name AS manager_name 
          m.salary AS manager_salary
from Employee e 
JOIN Employee m
       on e.manager_id = m.id
where e.salary > m.salary


# find the sql query max salary of employee under each manager  
select manager_id, name AS Emplyee_name
      salary
      from (select e*, row_number() over(
          partition by manager_id,
          order by salary DESC) AS rn
            from employee e
            where manager_id IS not null
) t
where rn =1;
